Parse a feed, match with reg exp and download the interesting feed entry.

Needs an config file config/config.yml

Example:

defaults:
  feed_uri: "http://www.ezrss.it/feed/"
  target_directory: "/target/directory"
  match_entries: ['unix']
